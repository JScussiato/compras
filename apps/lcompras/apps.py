# apps / lcompras / apps.py 
from django.apps import AppConfig

# input("Estou em apps / lcompras / apps.py")

class LcomprasConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.lcompras"
